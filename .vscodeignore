# Files to exclude from the extension package

# Screenshots and images not needed in the package
screenshot.jpg
screenshot.png
screenshots/

# Build outputs
*.vsix

# Git files
.git/
.gitignore

# VS Code folder
.vscode/

# Development files
*.log
npm-debug.log*
.DS_Store

# Optional
src/
.editorconfig
tsconfig.json

# Only the two docs the marketplace renders travel. Written as a PATTERN
# and not one by one: the file left out is always the NEXT one.
*.md
!README.md
!CHANGELOG.md

# Unpublished / WIP theme (not registered in package.json)
themes/dark-mode-hater.json

# Generated packaging artefact. `vsce` writes its own inside the .vsix, so the
# one at the root only travels to say a version that is not the one being cut.
.vsixmanifest

# The quality gate: not for the end user.
scripts/
